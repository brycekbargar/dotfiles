---
- name: Sync and update ports
  community.general.macports:
    selfupdate: true
    upgrade: true

- name: Install/replace macos tools
  become: true
  community.general.macports:
    name:
      - zsh
      - bash
      - git
      - gutils-suite
      - less
      - unzip
      - vim
    state: installed
